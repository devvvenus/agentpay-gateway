# Datasette fixture

Run this before starting the Datasette container:

```powershell
pnpm dataset:prepare
```

The script creates `demo.sqlite` with small hackathon/demo tables for the paid dataset adapter.
